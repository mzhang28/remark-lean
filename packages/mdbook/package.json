{
  "name": "@leandown/mdbook",
  "version": "0.0.20",
  "description": "mdbook plugin for syntax-highlighting Lean 4 code blocks via the Lean LSP",
  "module": "dist/index.js",
  "repository": {
    "url": "https://github.com/mzhang28/leandown"
  },
  "type": "module",
  "bin": {
    "mdbook-leandown": "./dist/main.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "prepack": "bun run build && chmod +x dist/main.js",
    "build": "bun build main.ts --outfile dist/main.js --packages external --target node && bun build runtime-entry.ts --outfile dist/index.js --target browser && cp ../core/lean.css dist/leandown.css"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@leandown/core": "workspace:*",
    "cmd-ts": "^0.15.0",
    "markdown-it": "^14.3.0"
  }
}
